[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
filterwarnings =
    ignore::DeprecationWarning:passlib.*
markers =
    integration: requires a live PostgreSQL database (provided by CI services)
